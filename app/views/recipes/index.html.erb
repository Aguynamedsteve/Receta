<h1>All Recipes</h1>

<ul>
  <% @recipes.each do |recipe| %>
    <li><%= link_to recipe.title, recipe %></li>
  <% end %>
</ul>

<%= link_to 'Home', home_index_path %> | <%= link_to 'New Recipe', new_recipe_path %>  