<h1><%= @recipe.title %></h1>

  <div>
    <%= form_for @recipe do |f| %>
      Title: <%= f.text_area :title %><br />
      Recipe: <%= f.text_area :body %><br />
      <%= f.submit %>
    <% end %>
  </div>

  <%= link_to "Back", :back %> | <%= link_to "Delete", method: :destroy %>